// Inicialização das variáveis
const INDICE = 12;
let SOMA = 0;
let K = 1;

// Loop para calcular a soma
while (K < INDICE) {
    K = K + 1;
    SOMA = SOMA + K;
}

// Imprimir o resultado
console.log(SOMA);

//Execute com (node soma.js)